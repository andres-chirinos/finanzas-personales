import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andreschirinos.billetera',
  appName: 'Billetera Personal',
  webDir: 'dist'
};

export default config;
